# frozen_string_literal: true

RSpec.describe Wacktrace do
  it "has a version number" do
    expect(Wacktrace::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
